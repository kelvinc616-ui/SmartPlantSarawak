import ManagePredictions from "../screens/ManagePredictions";

describe("ManagePredictions", () => {
  it("should be defined", () => {
    expect(ManagePredictions).toBeDefined();
  });

  it("should be a function or component", () => {
    expect(typeof ManagePredictions).toBe("function");
  });
});
